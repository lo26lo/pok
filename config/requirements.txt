# Requirements for Pokemon Dataset Generator
# ============================================
# Version 3.7 - Interface Qt (PySide6) + Albumentations (NumPy 2.x compatible)
#
# - Interface Qt par défaut (pyside6) ; Tkinter legacy sans dépendance extra
# - imgaug remplacé par Albumentations (compatible NumPy 2.x, plus rapide)
# - pandas >=2.2.2 (ABI NumPy 2.x — évite le crash _pandas_datetime_CAPI)

# Core - Pas de restriction de version (NumPy 2.x OK)
numpy>=1.24.0
opencv-python>=4.8.0
scipy>=1.11.0
scikit-image>=0.21.0

# Augmentation - Albumentations remplace imgaug
# <2.0 : l'API 2.x renomme/supprime des transformations utilisées
# (GaussNoise var_limit, ImageCompression quality_lower/upper, etc.)
albumentations>=1.3.0,<2.0

# Data formats
pyyaml>=6.0
pillow>=10.0.0
requests>=2.31.0

# GUI - Interface Qt (PySide6)
# Requis pour lancer GUI_qt.py (START.bat / start.sh).
# L'ancienne interface Tkinter (START_LEGACY.bat) n'en a pas besoin.
pyside6>=6.5

# pandas / openpyxl : mapping Excel legacy + scripts de migration.
# >=2.2.2 IMPÉRATIF : les versions <2.2.2 ne sont pas compilées contre
# NumPy 2.x et lèvent "_pandas_datetime_CAPI ... circular import" au démarrage.
pandas>=2.2.2
openpyxl>=3.1.0

# Image corruption effects (for augmentation)
imagecorruptions>=1.1.0
