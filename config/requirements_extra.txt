# Dépendances supplémentaires pour les nouvelles fonctionnalités
# Installation: pip install -r requirements_extra.txt

# === Pour l'entraînement YOLO ===
ultralytics>=8.0.0

# === Pour l'API REST ===
flask>=3.0.0
flask-cors>=4.0.0

# === Pour le mode collaboratif ===
SQLAlchemy>=2.0.0

# === Note: Albumentations est maintenant dans requirements.txt principal ===
# imgaug a été remplacé par albumentations (compatible NumPy 2.x)
